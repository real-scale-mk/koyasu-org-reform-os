# Purpose

このプロジェクトは、
「他人事化を突破する組織改革 Workspace」を実装するためのリポジトリ。

現象から構造を見抜き、
誰が何を引き受けるかを明確にし、
BABY STEPで改革を前に進めることを目的とする。

# Core Flow

Pane1: 現象把握
Pane2: 構造仮説
Pane3: 介入仮説
Pane4: 実行学習

現象
→ 構造仮説
→ 介入仮説
→ 実行学習
→ フィードバック

# Core Principles

- 事実と解釈を分ける
- 課題を他人事化しない
- 責任だけでなく権限・支援・後ろ盾も見る
- 本丸と最初の介入場所を分けて考える
- 改革を小さくするのではなく、実行単位を小さくする
- 実行結果から仮説と打ち手を更新する
- AIは判断代替ではなく、壁打ち・レビュー・知識補完に使う

# Development Rules

- V1の既存動作を壊さない
- 変更は小さく行う
- 重要変更はユーザー確認後に実行する
- 既存データやlocalStorage互換性を不用意に壊さない
- 変更後はテストとgit diffを確認する
- 新しい依存関係は勝手に追加しない

# Current Status

V1はfeature freeze済み。
今後はAI API、Subagent、Learning Memo、Skill化、DB等を段階的に追加する。

# Guiding Principle

地図を描く。
武器を選ぶ。
小さく試す。
結果から地図と武器を再構築する。